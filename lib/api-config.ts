/**
 * URL pública da API SportHub em produção (Render).
 * Na Vercel, defina `NEXT_PUBLIC_API_URL` com exatamente este valor (sem barra no final).
 */
export const PUBLIC_API_BASE_PRODUCTION = "https://sporthub-backend.onrender.com";
